{
  "type": "object",
  "required": ["redacted_text", "pii_found", "tags", "confidence"],
  "properties": {
    "redacted_text": { "type": "string" },
    "pii_found": { "type": "boolean" },
    "tags": {
      "type": "array",
      "items": { "type": "string", "enum": ["EMAIL", "PHONE", "SSN", "CARD", "ADDRESS", "NAME", "ACCOUNT_ID"] }
    },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
  },
  "additionalProperties": false
}

